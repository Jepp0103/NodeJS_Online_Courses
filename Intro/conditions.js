const x = 20;

if (x === 10) { //=== means it both checks if the value is the same datatype and if the value is the same.
    console.log("x", x);
} else {
    console.log("x is not 10");
}
console.log(x);